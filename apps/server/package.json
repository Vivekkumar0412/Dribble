{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild ./src/index.ts --bundle --platform=node --outdir=dist --format=cjs --loader:.ts=ts --sourcemap --minify --external:@prisma/client --external:express",
    "start": "node dist/index.js",
    "dev": "pnpm build && pnpm start",
    "remove-logs": "npx ts-node src/scripts/remove-logs",
    "lint": "eslint './src/**/*.{ts,js}'",
    "format": "prettier --write './src/**/*.{ts,tsx,js,jsx,json,md}'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@repo/database": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/jsonwebtoken": "^9.0.10",
    "dotenv": "^17.2.3",
    "@repo/types":"workspace:*",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "esbuild": "^0.27.2"
  }
}
